# Python 3 pipeline (scripts/*.py). Loose minimums: nothing here uses a recent API.
# scripts/01, 02, 03 and test_checks.py are standard library only.
numpy>=1.24        # 06/07/08: point-cloud and layout maths
matplotlib>=3.7    # 07: results/network_preview.png
openpyxl>=3.1      # 03: reads the paper's .xlsx supplementary files

# zstandard is NOT needed: scripts/02_scan_dataset.py decompresses the 2 GB
# dataset by shelling out to the `zstd` CLI (zstd -dcq), so install that instead
# (brew install zstd / apt-get install zstd).
